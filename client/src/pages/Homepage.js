import React from 'react'

import Layout from '../components/Layout'

export default function Homepage() {
    return (
        <Layout>
            <p>You're in the Homepage right now.</p>
        </Layout>
    )
}